import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParquecampodesantanaPage } from './parquecampodesantana.page';

describe('ParquecampodesantanaPage', () => {
  let component: ParquecampodesantanaPage;
  let fixture: ComponentFixture<ParquecampodesantanaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParquecampodesantanaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
