import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParquequintadaboavistaPage } from './parquequintadaboavista.page';

describe('ParquequintadaboavistaPage', () => {
  let component: ParquequintadaboavistaPage;
  let fixture: ComponentFixture<ParquequintadaboavistaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParquequintadaboavistaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
